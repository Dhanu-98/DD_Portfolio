"use client";import{useEffect,useState}from"react";import{useRouter}from"next/navigation";const pages=["/","/about","/experience","/projects","/skills","/achievements","/certifications","/resume","/contact"];export function CommandPalette(){const[open,setOpen]=useState(false);const router=useRouter();useEffect(()=>{const f=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key==="k"){e.preventDefault();setOpen(x=>!x)}};const g=()=>setOpen(true);addEventListener("keydown",f);addEventListener("open-command",g);return()=>{removeEventListener("keydown",f);removeEventListener("open-command",g)}},[]);if(!open)return null;return <div className="fixed inset-0 z-[60] bg-black/60 backdrop-blur-sm grid place-items-start pt-[18vh] px-5" onClick={()=>setOpen(false)}><div className="glass rounded-2xl w-full max-w-lg p-3" onClick={e=>e.stopPropagation()}><p className="px-3 py-2 text-xs text-zinc-500">Jump to a page</p>{pages.map(p=><button key={p} onClick={()=>{router.push(p);setOpen(false)}} className="w-full text-left px-3 py-3 rounded-lg hover:bg-white/10 text-sm">{p==="/"?"Home":p.slice(1).replace("-"," ")}</button>)}</div></div>}
