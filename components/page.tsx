import Link from"next/link";import{ArrowUpRight}from"lucide-react";export function PageHero({eyebrow,title,children}:{eyebrow:string,title:React.ReactNode,children?:React.ReactNode}){return <main className="pt-28"><section className="section pb-12"><p className="eyebrow">{eyebrow}</p><h1 className="heading mt-4 max-w-4xl">{title}</h1>{children&&<div className="mt-7 max-w-2xl muted text-lg">{children}</div>}</section></main>}export function Back(){return <Link href="/projects" className="button button-ghost mt-8">← All projects</Link>}
