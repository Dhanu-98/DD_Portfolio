"use client";import Link from "next/link";import{Menu,X,Command}from"lucide-react";import{useState,useEffect}from"react";const links=["About","Experience","Projects","Skills","Achievements","Contact"];export function Navbar(){const[open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false);useEffect(()=>{const f=()=>setScrolled(scrollY>20);addEventListener("scroll",f);return()=>removeEventListener("scroll",f)},[]);return <header className={`fixed top-0 z-40 w-full transition ${scrolled?"bg-[#07070b]/75 backdrop-blur-xl border-b border-white/10":""}`}><nav className="max-w-6xl h-16 mx-auto px-5 flex items-center justify-between"><Link href="/" className="font-semibold tracking-tight">DD<span className="text-violet-400">.</span></Link><div className="hidden md:flex gap-6 text-sm text-zinc-400">{links.map(x=><Link className="hover:text-white transition" href={`/${x.toLowerCase()}`} key={x}>{x}</Link>)}</div><div className="flex items-center gap-3"><button onClick={()=>window.dispatchEvent(new Event("open-command"))} className="hidden sm:flex text-xs text-zinc-400 border border-white/10 rounded-md px-2.5 py-1.5 gap-2"><Command size={13}/>K</button><button className="md:hidden" onClick={()=>setOpen(!open)} aria-label="Toggle navigation">{open?<X/>:<Menu/>}</button></div></nav>{open&&<div className="md:hidden glass p-5 grid gap-4 text-sm">{links.map(x=><Link onClick={()=>setOpen(false)} href={`/${x.toLowerCase()}`} key={x}>{x}</Link>)}</div>}</header>}
